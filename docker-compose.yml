services:
  mongo:
    image: mongo:8.0
    ports:
      - "${MONGO_PORT:-27018}:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      - MONGODB_INITDB_DATABASE=github-research
      - GLIBC_TUNABLES=glibc.cpu.hwcaps=-SHSTK

volumes:
  mongo-data:
